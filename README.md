# Samarizator

Десктопное приложение для macOS: превращает аудио и видео в содержательные заметки с собеседниками, решениями, задачами и таймкодами. Распознавание происходит локально через **Whisper.cpp**, сводки — через **ваш корпоративный API**. База знаний — папка Markdown для **Obsidian с графом связей**.

## Запуск на Mac

Нужен macOS 13+ и [Homebrew](https://brew.sh). Готовые пакеты Homebrew доступны не на всех старых версиях macOS; для максимально простой установки используйте поддерживаемую Homebrew версию системы. Поддерживаются Apple Silicon и Intel; скорость зависит от процессора.

```bash
git clone https://github.com/Igibaev/samarizator.git && cd samarizator
./start.sh
```

Первый запуск устанавливает uv, FFmpeg, Whisper.cpp и Python-зависимости, скачивает модели и открывает окно. Нужен интернет для установки; модели занимают несколько сотен МБ, зависимости — дополнительное место. Последующие запуски используют готовое окружение и модели. Если Homebrew ещё нет, установите его по инструкции на brew.sh и повторите команду.

В **Настройки → Корпоративная модель** один раз укажите:

1. **Полный HTTPS endpoint** Chat Completions вашего корпоративного шлюза. Не адрес страницы чата.
2. **Имя модели**, доступной вашему аккаунту.
3. **API-ключ** — сохраняется в macOS Keychain, не в Git и не в настройках.
4. При необходимости — заголовок `api-key` вместо `Authorization`, пустой префикс вместо `Bearer ` и файл корпоративного CA.

Адрес и ключ нельзя предзаполнить без данных вашей компании. Публичного API или запасной облачной модели в приложении нет. OAuth/SSO-only веб-чат без API напрямую не поддерживается.

## Как пользоваться

1. Добавьте локальный аудио- или видеофайл: MP3, MP4, M4A, WAV, MOV, MKV, WebM, FLAC и другие форматы, которые читает FFmpeg. Используется первая аудиодорожка.
2. Нажмите **Распознать / продолжить**. Аудио остаётся на Mac.
3. Проверьте отмеченные реплики. Переименуйте «Собеседник 1» в настоящее имя; при необходимости примените имя ко всем его репликам. Исправьте ошибки текста.
4. Нажмите **Создать сводку**. Только на этом этапе текст отправляется на заданный корпоративный endpoint.
5. Откройте результат через **Открыть в Obsidian**. При первом использовании откройте `~/Documents/Samarizator` в Obsidian как существующее хранилище. Obsidian устанавливается отдельно; без него заметки доступны в любом Markdown-редакторе.

Можно открыть исходное медиа в системном проигрывателе и свериться с показанным таймкодом. Встроенного проигрывателя с переходом по таймкоду в этой версии нет.

### Что входит в сводку

- Краткий итог и основные темы — без последовательного пересказа всех реплик.
- Решения; задачи с ответственными и сроками, **только если они названы**.
- Риски, разногласия и открытые вопросы.
- Ссылки на конкретные фрагменты расшифровки с таймкодами.
- **Подробный реестр пунктов** из всех исходных блоков до финального сокращения. Это позволяет проверить темы, которые не вошли в короткую сводку.

Запись проходит по всем блокам, а не обрезается под контекст модели. Синтез объединяет результаты в несколько уровней. Ответы с несуществующими ID фрагментов, повреждённым JSON или признаком обрезания отклоняются. Проверка ID подтверждает существование источника, но не доказывает истинность каждого утверждения: модель всё ещё может ошибиться. Абсолютная полнота и безошибочность автоматической сводки не гарантируются.

## Собеседники

| Режим | Для чего | Ограничения |
|---|---|---|
| Автоматический, по умолчанию | Обычная запись встречи в одном канале | Дополнительные **локальные** ONNX-модели сегментации и голоса через sherpa-onnx; автоматические метки нужно проверить |
| Два отдельных канала | Интервью, где один человек только слева, другой только справа | Обычная стереозапись комнаты для этого не подходит; распознаётся каждый канал отдельно |
| Ручной, только Whisper | Строго без дополнительных моделей | Whisper распознаёт текст; говорящих назначаете вручную |

Whisper сам по себе не обеспечивает надёжную идентификацию говорящих в общей записи. Поэтому автоматический режим использует отдельный локальный модуль. Он не распознаёт текст и не отправляет аудио в сеть. Номера говорящих стабильны внутри записи, но не идентифицируют человека между встречами. Одновременная речь, шум, короткие реплики и похожие голоса снижают точность. Используемая модель embedding обучалась на китайской речи; качество на русскоязычных встречах требует проверки. Можно указать другую совместимую модель sherpa-onnx в настройках.

Для строгой установки без дополнительного модуля:

```bash
SAMARIZATOR_STRICT=1 ./start.sh
```

## Память и длительные записи

По умолчанию: **4 ГиБ**, 4 потока CPU, фрагменты по 120 секунд, квантованная мультиязычная `small-q5_1`. Whisper запускается с `-ng`: GPU/Metal выключен, чтобы не скрывать потребление в общей GPU-памяти. Это медленнее GPU.

- Одна задача за раз. FFmpeg извлекает только очередной фрагмент; Whisper не загружает всё медиа.
- Отдельные процессы для тяжёлых стадий. Модели диаризации освобождаются перед Whisper.
- Контроль суммарного RSS приложения и дочерних процессов каждые 100 мс.
- Защитная остановка при **90% выбранного бюджета**; запас предназначен для колебаний между проверками.
- Фрагменты и сводки блоков сохраняются транзакционно в SQLite. После остановки можно продолжить. Текущий незавершённый фрагмент выполняется повторно.
- При повторном запуске проверяется SHA-256 исходного файла: изменённое медиа нельзя незаметно смешать с прежней расшифровкой.

**Это не жёсткая квота оперативной памяти на уровне macOS.** Быстрое выделение памяти может кратковременно превысить порог до следующей проверки. RSS не учитывает весь системный файловый кеш; внешние приложения, Homebrew и установка моделей не входят в бюджет. Режим гарантированного жёсткого потолка потребовал бы отдельной VM/контейнера и не входит в эту версию.

Автоматическое определение голосов анализирует запись целиком для согласованной нумерации. Его память растёт с длительностью; предварительная оценка отклонит слишком длинную запись для выбранного бюджета, а watchdog контролирует фактический расход. Для таких записей увеличьте бюджет или импортируйте заново в режиме каналов/ручном. Не выделяйте приложению всю память Mac.

На границах фрагментов используется перекрытие 2 секунды. Пограничные реплики помечаются для проверки: возможны повторы или различия распознавания на стыке. Это не гарантия пословного выравнивания.

## База знаний

```text
Samarizator/
  Meetings/       # сводки, задачи, решения, ссылки на темы
  Transcripts/    # расшифровки с говорящими, временем и ID фрагментов
  Topics/         # общие узлы тем; встречи связаны через них
```

Obsidian строит граф по `[[ссылкам]]`; отдельная графовая БД не нужна. Темы с одинаковым нормализованным названием связываются автоматически. Синонимы не объединяются магически — их можно объединить вручную в Obsidian. Никакой автоматической загрузки в Obsidian Sync нет.

Экспорт обновляется автоматически после успешной сводки. Если заметка или расшифровка были изменены вручную, следующая выгрузка создаст новую пару файлов и сохранит старую. Изменение расшифровки в приложении сбрасывает сводку: её нужно пересоздать. Старые экспортированные версии остаются в хранилище для истории.

## Данные и приватность

- Медиа: исходный файл не копируется и не удаляется.
- Состояние, расшифровки, настройки, модели: `~/Library/Application Support/Samarizator`.
- Заметки: `~/Documents/Samarizator` либо выбранная папка.
- Секрет: macOS Keychain; для автоматизации можно задать `SAMARIZATOR_API_KEY`.
- SQLite и Markdown не зашифрованы приложением. При необходимости используйте FileVault и корпоративные правила хранения.
- Нет телеметрии, облачного ASR, публичного fallback, автоматических редиректов или чтения системных HTTP-прокси. Корпоративный VPN поддерживается на уровне сети ОС. Если нужен обязательный HTTP-прокси, потребуется адаптация клиента.
- Аудио не отправляется ни в корпоративный API, ни в модельные хостинги. Загрузки моделей обращаются к Hugging Face/GitHub только за весами.
- Временные WAV и технические логи удаляются после обработки, а оставшиеся после аварийного выхода — при следующем запуске.

Репозиторий содержит только приложение, тесты и документацию. В нём нет ключей, записей, моделей или корпоративных материалов.

## Разработка и проверка

```bash
uv sync --frozen --extra diarization --extra dev
uv run --frozen --extra diarization --extra dev pytest -q
uv run --frozen --extra dev ruff check src tests
```

Для Linux headless задайте `QT_QPA_PLATFORM=offscreen`. CI запускает проверки на Linux и macOS; отдельный macOS smoke-test использует настоящий FFmpeg, Whisper и локальную диаризацию. Корпоративный API в тестах заменён контролируемым HTTP-транспортом — реальные корпоративные ключи CI не нужны.

[Архитектура](docs/architecture.md) · [Устранение проблем](docs/troubleshooting.md) · [Отчёт проверок](docs/validation.md) · [Зависимости и модели](docs/models.md)
